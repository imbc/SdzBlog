<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Latest Post<b class="caret"></b></a>
    <ul class="dropdown-menu">
    {% for post in posts %}
        <li>
            <a href="{{ path( 'sdzblog_view', { 'post_id': post.id } ) }}">{{ post.titre }}</a>
        </li>
    {% endfor %}
    </ul>
</li>

